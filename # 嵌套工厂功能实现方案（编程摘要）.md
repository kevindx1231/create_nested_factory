## 🤓 模组方案

# 嵌套工厂功能实现方案（编程摘要）

## 1. 功能规则

1. 根工厂可正常进入、退出、扩展和坍缩。
2. 在任意有效工厂空间内部可放置嵌套工厂方块。
3. 每个工厂空间中，**仅第一个有效嵌套工厂可进入**。
4. 同一空间内后续放置的嵌套工厂为“无效嵌套方块”：
   - 不可进入；
   - 不分配空间；
   - 不注册端口、应力、黑盒、区块票等运行逻辑。
5. 有效嵌套工厂可继续嵌套，形成父 → 子 → 孙工厂链。
6. 根工厂以外的全部嵌套工厂：
   - 固定默认空间尺寸（如 `16×16×16`）；
   - 禁止扩展；
   - 禁止坍缩。
7. 嵌套空间使用专用槽位区域，整体位于常规根工厂空间区域下方；不使用同一 X/Z 的纯垂直堆叠。
8. 建议设置逻辑最大嵌套深度，默认建议 8 层，可配置上限 16 层。

---

# 2. 空间布局与槽位分配

## 2.1 根工厂空间

根工厂继续沿用现有的空间映射和扩展逻辑。

每个根工厂保存：

- 工厂唯一 ID；
- 根空间原点及当前边界；
- 当前空间尺寸；
- 是否为根工厂；
- 可进入直属子工厂 ID/位置（最多一个）。

## 2.2 嵌套工厂空间

所有有效嵌套工厂由全局“嵌套槽位分配器”分配独立空间。

嵌套槽位区域应具备：

- 固定的嵌套区起始坐标；
- 固定槽位尺寸，例如 `32×32×32`；
- 固定房间实际尺寸，例如 `16×16×16`；
- 每个槽位间留出墙体和安全空隙；
- 嵌套区整体 Y 坐标低于根工厂正常空间区域。

每个有效嵌套工厂必须持久化保存：

- 工厂唯一 ID；
- `isNested = true`；
- `enterable = true`；
- 嵌套深度；
- 父工厂 ID；
- 根工厂 ID；
- 槽位编号；
- 槽位坐标 `slotX / slotZ`；
- 实际房间原点；
- 固定房间边界；
- 房间生成状态/版本；
- 当前直属可进入子工厂 ID（最多一个）。

不要仅通过“深度”实时推导空间坐标；必须保存实际分配的槽位和房间原点。

---

# 3. 双索引方案

采用“双索引”处理根工厂与嵌套工厂的空间归属查询。

## 3.1 嵌套工厂：槽位哈希索引

嵌套工厂使用固定网格槽位，索引结构：

- Key：`dimension + slotX + slotZ`
- Value：`factoryId`

查询位置是否处于嵌套空间时：

1. 根据坐标和嵌套区原点计算 `slotX / slotZ`；
2. 用槽位 Key 直接查询有效嵌套工厂；
3. 查询成功后，对该工厂做精确的三维边界判断；
4. 边界命中才确认位置属于该工厂。

该查询应为 O(1)，不能遍历所有嵌套工厂。

注意负坐标计算必须使用向下取整，不能使用会向零截断的普通整数除法。

## 3.2 根工厂：区域桶或区块索引

根工厂可扩展，边界不固定，因此使用区域桶索引或区块索引。

推荐结构：

- Key：`dimension + regionX + regionZ`
- Value：该区域可能覆盖到的根工厂 ID 列表。

区域建议大于根工厂可能的常见尺寸，例如 `256×256` 或 `512×512`。

查询位置是否属于根工厂时：

1. 计算所在区域；
2. 查找该区域登记的根工厂候选列表；
3. 对候选根工厂做精确三维边界判断；
4. 命中即返回对应根工厂。

根工厂扩展或坍缩后，必须更新其区域桶登记和边界版本号。

## 3.3 查询顺序

统一的“位置 → 所属工厂”查询流程：

1. 判断目标维度是否为口袋维度；
2. 若坐标位于嵌套专用区域，优先查询嵌套槽位索引；
3. 槽位命中后进行精确边界校验；
4. 未命中时，查询根工厂区域桶索引；
5. 对根工厂候选进行精确边界校验；
6. 均未命中则视为不在工厂空间中。

所有最终判断都必须以实际三维房间边界为准，索引只负责快速缩小候选范围。

---

# 4. 放置和有效性判定

## 4.1 在外部世界放置

在非口袋维度放置工厂方块：

- 创建根工厂；
- `depth = 0`；
- 可进入；
- 可扩展、可坍缩；
- 登记根工厂索引。

## 4.2 在有效工厂内部放置

当在口袋维度放置嵌套工厂方块：

1. 用双索引查找当前位置所属父工厂；
2. 仅允许在父工厂“内部可建造区域”放置，不允许墙体、顶底外壳和房间外区域；
3. 检查父工厂是否已有有效直属子工厂。

若父工厂没有有效直属子工厂：

- 检查嵌套深度上限；
- 从全局槽位分配器申请槽位；
- 创建有效嵌套工厂；
- 记录父子关系；
- 父工厂写入直属子工厂引用；
- 写入槽位索引；
- 加载并生成目标房间；
- 固定空间尺寸。

若父工厂已有有效直属子工厂：

- 当前方块标记为不可进入；
- 不申请槽位；
- 不生成空间；
- 不加入空间索引；
- 不启动端口、应力、黑盒及区块票逻辑；
- 交互时显示“该工厂空间已有可进入的嵌套工厂”。

## 4.3 不能只依赖放置事件

必须在以下时机校验嵌套工厂状态：

- 正常玩家放置；
- 方块实体加载；
- 方块被命令、结构或其他模组生成；
- 方块交互；
- 工厂存档读取；
- 父工厂或子工厂被拆除后。

防止活塞、复制、命令或区块加载顺序绕过“第一个有效”的规则。

---

# 5. 进入、退出与返回栈

## 5.1 玩家工厂探索会话

玩家第一次从非工厂环境进入根工厂时，创建“工厂探索会话”。

会话保存：

- 根工厂 ID；
- 当前工厂 ID；
- 逐层返回栈；
- 原始游戏模式；
- 原始飞行权限和飞行状态；
- 原始夜视状态；
- 工厂临时飞行是否由本模组授予；
- 工厂夜视是否由本模组提供。

## 5.2 返回栈

每次进入一个工厂时压入来源记录：

- 来源维度；
- 来源位置；
- 来源视角；
- 来源工厂 ID（若存在）；
- 目标工厂 ID。

行为：

- 外部世界 → 根工厂：压入外部位置；
- 根工厂 → 第一层嵌套工厂：压入根工厂当前位置；
- 子工厂 → 更深子工厂：压入当前子工厂位置；
- 退出当前工厂：弹出一条记录并返回其来源位置。

## 5.3 完全退出条件

只有玩家从根工厂退出、且返回栈下一位置不属于工厂链时，才结束探索会话。

从嵌套工厂返回父工厂时，不结束会话。

---

# 6. 飞行与夜视

## 6.1 飞行

飞行属于整条工厂探索会话，不属于单个工厂层。

首次进入根工厂时：

- 保存玩家原始飞行权限与飞行状态；
- 若玩家本来没有飞行权限，则授予“工厂临时飞行”；
- 可让玩家立即处于飞行状态，避免传送后跌落。

在根工厂和嵌套工厂之间切换时：

- 不重复授予；
- 不撤销；
- 不重复同步能力。

完全退出工厂链时：

- 仅撤销本模组授予的临时飞行；
- 恢复进入前记录的飞行状态；
- 不影响创造、旁观或其他模组提供的飞行；
- 返回点不安全时，应先完成安全传送，并提供短暂无摔落保护后再撤销飞行。

## 6.2 夜视

夜视同样属于探索会话。

规则：

- 首次进入根工厂时提供工厂夜视；
- 父子工厂间传送时不重复施加；
- 每 5～10 秒低频检查和续期；
- 不要每 tick 重加药水效果；
- 只移除明确属于本模组的夜视；
- 不无条件清除玩家已有的药水、信标、命令或其他模组夜视；
- 完全退出、死亡或异常离开时停止续期并清理工厂来源夜视。

---

# 7. 区块加载与引用计数

每个工厂房间的区块加载使用引用计数，不采用简单的单一开关。

建议按来源区分：

- `playerRefCount`：玩家位于或即将传送至该空间；
- `generationRefCount`：房间生成、修复或校验中；
- `blackBoxRefCount`：黑盒运行中；
- `transitionRefCount`：传送过程中的短期加载引用。

规则：

1. 任意引用计数大于零，即保留对应内部区块加载。
2. 对嵌套工厂，还要保证父空间中该工厂方块所在的外部区块已加载。
3. 所有引用归零后，延迟一段时间再释放区块票，避免频繁进出造成加载抖动。
4. 区块票必须能在拆除、死亡、异常传送、断线和服务器重启后清理。

---

# 8. 玩家位置缓存

每位玩家缓存当前空间归属：

- 当前工厂 ID；
- 空间类型：根工厂 / 嵌套工厂 / 外部；
- 当前维度；
- 当前区块坐标；
- 对应工厂边界版本号。

只在以下情况重新通过双索引定位：

- 玩家跨区块；
- 玩家切换维度；
- 玩家被传送；
- 玩家登录；
- 当前工厂被拆除或失效；
- 当前工厂边界版本改变；
- 缓存边界校验失败。

普通移动、飞行处理、夜视维护等不应重复进行全局空间查询。

---

# 9. 槽位分配器

建立服务端全局、持久化的槽位分配器。

需保存：

- `nextSlotId`；
- 已占用槽位集合；
- `slotKey -> factoryId` 槽位索引；
- 可选的已安全清理槽位复用队列；
- 槽位版本或分配时间信息，用于排错和数据修复。

分配流程：

1. 优先使用已安全回收的槽位；
2. 没有可回收槽位时，使用 `nextSlotId`；
3. 立即标记槽位占用；
4. 创建并保存工厂数据；
5. 写入槽位索引；
6. 加载目标区块；
7. 幂等生成或校验目标房间；
8. 最后允许玩家进入。

不要扫描所有现有工厂来寻找空槽位。

---

# 10. 拆除、失效和孤立后代

## 10.1 拆除有效嵌套工厂

拆除一个有效子工厂后：

- 从父工厂清除直属子工厂引用；
- 从槽位索引移除该工厂；
- 释放或标记其槽位；
- 清理端口、应力、黑盒、区块票等注册；
- 允许父工厂后续绑定新的有效嵌套工厂。

## 10.2 有后代时的推荐策略

若被拆除的嵌套工厂拥有更深层有效子工厂，推荐采取以下保守策略：

- 禁止直接拆除；
- 提示“该嵌套工厂存在下级工厂，无法拆除”。

这样避免后代空间、入口、端口和返回链变成孤立数据。

如未来需要支持拆除，可增加专门的“递归回收/解除嵌套”功能，而不是直接允许破坏。

## 10.3 无效嵌套方块

无效额外嵌套方块拆除时：

- 不影响父工厂的有效子工厂引用；
- 不触发槽位释放；
- 不需要任何空间清理。

---

# 11. 端口、应力与黑盒兼容要求

所有涉及“工厂内部位置”的系统都必须改为使用工厂记录的**实际房间原点和边界**，不能再默认由外部方块坐标推导内部位置。

需要检查并适配：

- 传送入口与出口；
- 房间生成和墙体维护；
- 端口内部/外部映射；
- 应力端口和动能网络；
- 黑盒模式扫描范围；
- 黑盒运行区块加载；
- 方块实体注册和注销；
- 玩家死亡、登出、重连定位；
- 房间内安全位置检测。

根工厂和嵌套工厂统一通过“工厂实例 → 房间边界/房间原点”接口获取位置数据。

---

# 12. 异常恢复与存档校验

在服务器启动、世界加载和玩家登录时执行校验：

- 工厂 ID 是否唯一；
- 槽位是否被多个工厂占用；
- `slotKey -> factoryId` 是否与工厂存档一致；
- 父工厂的直属子工厂引用是否存在且有效；
- 子工厂父链是否存在循环；
- 嵌套深度是否超过上限；
- 无效工厂是否错误持有槽位、端口或区块票；
- 玩家会话是否与当前位置一致；
- 区块票引用是否存在无主残留。

父链遍历必须设置：

- 最大深度限制；
- 已访问工厂 ID 集合；

避免数据损坏时产生父子循环和无限递归。

---

# 13. 推荐实现顺序

1. 定义根工厂、有效嵌套工厂、无效嵌套方块的数据模型。
2. 实现全局持久化槽位分配器。
3. 实现嵌套槽位哈希索引。
4. 实现根工厂区域桶/区块索引。
5. 实现统一的双索引空间归属查询。
6. 实现有效嵌套工厂的放置、绑定、槽位分配和房间生成。
7. 实现无效额外嵌套方块的轻量化逻辑。
8. 修改所有内部坐标相关功能，统一读取实际房间原点。
9. 实现多层进入、逐层返回栈和探索会话。
10. 实现飞行与夜视的会话级管理。
11. 实现区块票引用计数和延迟释放。
12. 实现拆除、父子解绑、后代保护和残留清理。
13. 完成存档迁移、重启校验和异常恢复。
14. 最后进行端口、应力、黑盒、多玩家和性能压力测试。
