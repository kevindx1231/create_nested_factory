# 嵌套工厂领域语境

本项目将外部世界与 Pocket 工厂空间连接，并在真实运行、学习、黑盒和蓝图模式之间保持资源传输语义一致。

## Language

**物流驱动传输**:
正常运行中，每一次资源跨工厂边界的移动都由玩家布置的物流设备发起；工厂边界本身不主动抽取或推送资源。
_Avoid_: 自动搬运、主动泵送

**同步直通**:
未配置物流交接缓冲的资源路径必须在一次能力调用中完成跨维度交付；对侧不能立即接受或提供时，调用原样失败。
_Avoid_: 隐式延迟转发、后台搬运、未声明的中转存储

**完整输出承诺**:
生产提交后，当前批次的全部产物都成为不可丢失、可由物流分次领取的确定承诺；全部交付前不能开始下一批次。
_Avoid_: 部分生产、溢出丢弃、主动推送

**资源传输路径**:
物品和流体穿过工厂边界的统一领域行为，适用于常加载、排空、学习、黑盒和蓝图模式。
_Avoid_: 仅学习通道、仅黑盒通道

**资源缓存**:
可被物流或玩家作为库存或储罐读取、填充或提取的通用资源存储；除明确限定用途和生命周期的物流交接缓冲外，工厂边界不得拥有这种存储。
_Avoid_: 通用端口仓库、中转罐、无限仓库、未声明容量的存储

**端口组交接缓冲**:
由一个端口 ID 的全部内部端口共享的九槽物品状态，用于让边界两侧物流在不同 tick 完成交付；端口为 INPUT 时接受外部写入并供内部提取，端口为 OUTPUT 时接受内部写入并供外部提取。物品成功写入时即完成该方向的边界传输和学习记账；增加同 ID 内部端口不能增加容量或复制资源。
_Avoid_: 每端口独立库存、输入输出双缓冲、跨端口共享、写入后重复记账

**直通优先交接**:
一次边界传输先尝试由对侧标准物流端点同步接收，未能立即交付的剩余物品再写入对应端口组交接缓冲；同步交付量与缓冲接收量合并为本次唯一传输记录。
_Avoid_: 缓冲必经、重复统计、缓冲满时静默丢弃

**生产批次状态**:
与一份确定配方绑定、不可作为库存或储罐访问且最多容纳一个生产批次的统一资源承诺；全部物品和流体输入、功率和周期共同决定一次原子提交。
_Avoid_: 资源缓存、通用库存、独立物品批次、独立流体批次

**批次物化**:
模式、蓝图、端口配置或方块生命周期使物品批次失效时，将已提交的原始输入或尚待交付的完整输出转换为工厂位置的世界掉落物；这是允许绕过输出物流的显式生命周期例外。
_Avoid_: 跨配方迁移、静默删除、普通库存退款

**流体批次销毁**:
任何模式、蓝图、端口配置、方块生命周期或损坏数据恢复使批次失效时，已提交或尚待交付的流体不退款、不物化且不迁移，直接从世界状态中移除。
_Avoid_: 自动回填、桶装退款、流体方块生成

**虚拟资源位**:
生产批次状态通过按注册 ID 稳定排序的物品槽和流体 tank 向物流展示剩余需求或待交付承诺，但不提供批次之外的储存容量。
_Avoid_: 动态混合槽、通用库存、固定六槽限制

**接入面**:
物流设备向工厂或内部端口请求资源能力时所接触的具体方块面；无方向能力请求不属于有效接入。
_Avoid_: 固定朝向、邻居扫描结果、无方向访问

**标准物流端点**:
在接触面暴露 NeoForge 物品或流体方块能力，或主动调用相邻能力的物流设备；工厂不按具体模组或方块类名识别物流。
_Avoid_: Create 特判、物流类名白名单

**方向语义**:
INPUT 只允许资源从外部进入工厂空间，OUTPUT 只允许资源从工厂空间离开，NONE 不允许资源通过。
_Avoid_: 双向端口、统计标签

**接入面竞争**:
多个调用方共享同一资源状态，最先成功完成能力调用的物流设备取得本次传输。
_Avoid_: 每面独立副本

**端口组**:
同一工厂空间内映射到相同端口 ID 的多个内部端口集合；它们共享同一个外部工厂面，并共同参与物品和流体路由。
_Avoid_: 重复端口冲突、每端口资源副本

**端口组轮询**:
端口组为物品插入、物品提取、流体填充和流体排出分别持久化游标，轮换同步传输的起始目标，再依次尝试其余端口和接入面。
_Avoid_: 固定首选端口、随机路由、跨资源共享游标

**镜像压力**:
同一端口组的所有流体端口获得相同的外部泵压力；压力只表示流动驱动力，实际 mB 数量由共享传输协调器守恒结算。
_Avoid_: 压力容量、平均分压、每端口流体副本

**生产周期**:
学习结果规定的最短批次提交间隔；物流可以提前提交当前批次输入，但不能突破该间隔提高黑盒产能。
_Avoid_: 端口限速、物流冷却

**无配额吞吐**:
工厂不设置每 tick、每面或每端口的传输额度；实际传输量受调用参数、对侧接受能力和当前批次剩余量约束，并使用溢出检查保证数值安全。
_Avoid_: 数学无限、整数溢出、固定速率上限

**外部返回锚点**:
会话栈中 sourceFactoryId 为空的最早返回帧，表示进入任何 Pocket 工厂树前的外部世界位置；会话故障恢复必须以它为候选目的地。
_Avoid_: 栈顶返回帧、父工厂房间、当前 Pocket 位置
**孤立 Pocket 会话**:
玩家 attachment 中仍为 active、但登录时当前 Pocket 位置无法解析为同一工厂树的会话；必须结束临时能力并执行故障恢复。
_Avoid_: 静默恢复失败、残留会话

**精确锚点恢复**:
孤立会话恢复时精确传送到外部返回锚点记录的维度、坐标和视角；仅在锚点缺失或维度不存在时回退至主世界出生点。
_Avoid_: 安全落点搜索、总是主世界出生点

**已验证 Pocket 会话**:
当前 Pocket 位置、currentFactoryId、rootFactoryId 与工厂运行状态均一致的 active 会话；只有这种会话可以恢复在线引用和临时探索能力。
_Avoid_: 仅 ID 匹配、任意 Pocket 房间

**登录故障恢复**:
在 PlayerLoggedInEvent 中同步处理孤立会话：必要时立即离开 Pocket，恢复原能力并清空会话，避免普通玩家 tick 先在损坏房间执行。
_Avoid_: 延迟一 tick、移动时恢复

**会话工厂引用**:
FactorySession 持久化的当前工厂维度、完整 BlockPos、工厂 ID 和根工厂 ID；登录恢复通过该引用加载并验证工厂，而不是依赖在线运行时注册表。
_Avoid_: 玩家当前位置查找、运行时注册表唯一真相

**无会话 Pocket 玩家**:
位于 Pocket 维度但没有已验证 Pocket 会话的玩家；登录时必须送往主世界出生点，且不能自动获得探索能力。
_Avoid_: 原地保留、自动绑定最近工厂

**可验证返回帧**:
非外部 ReturnFrame 持久化其来源工厂的维度、完整 BlockPos 和根工厂 ID；退出或重连时必须验证整条帧栈，任一帧失效即使用外部返回锚点恢复。
_Avoid_: 仅 ID 返回帧、盲目坐标传送

**登录验证加载**:
登录恢复仅同步加载会话当前工厂引用所在的一个区块，用于验证当前工厂及其持久化根工厂身份；验证后不新增持续强加载。
_Avoid_: 加载整棵工厂树、依赖恰好已加载的区块

**失败可恢复传送**:
进入、退出和登录恢复中的 attachment 更新与传送按可恢复顺序执行；登录发现会话与玩家实际位置不一致时，必须清空会话或返回外部锚点。
_Avoid_: 传送后才保存、传送前清空全部状态

**会话恢复隔离**:
验证加载、工厂读取和传送异常不能冒泡阻塞登录；恢复逻辑在异常后清理临时能力、清空会话，并回退至主世界出生点。
_Avoid_: 登录异常传播、无限重试会话

**菜单工厂上下文**:
服务端 FactoryMenu 持有的具体工厂方块实体及容器 ID；菜单相关操作必须通过该上下文定位工厂，不能信任客户端发送的位置坐标。
_Avoid_: 客户端坐标寻址、方块坐标改名包

**无损菜单数值**:
经由 ContainerData 传输的完整数值必须拆分为可无损重建的 16 位片段；客户端不得把单个数据槽当作完整坐标、浮点数或其他 32 位值。
_Avoid_: 单槽浮点缩放、截断坐标

**菜单操作范围**:
菜单操作仅在玩家与绑定工厂位于同一维度、距离不超过 8 格且方块实体实例仍有效时执行。
_Avoid_: 远程菜单、仅存在性校验

**无损菜单资源摘要**:
菜单显示的物品或流体注册 ID 与速率一样以 16 位片段传输，并在客户端重建完整 32 位值。
_Avoid_: 单槽资源 ID、截断图标

**菜单上下文改名**:
改名请求只引用当前容器 ID；服务端从玩家当前有效的 FactoryMenu 取得工厂，并在距离、实例和名称长度校验通过后执行改名。
_Avoid_: 坐标改名包、客户端名称校验、远程改名

**运行时配置**:
暴露给服务器或玩家、且确实改变嵌套工厂机制的配置项；当前仅包含最大嵌套深度。
_Avoid_: 模板示例配置、未来占位字段

**零容量空转**:
外部动力来源有效且内部应力需求为零时，应力端口维持选定来源的转速，将零需求视为已满足，但不占用外部应力，也不向内部提供可消耗的 SU 容量。
_Avoid_: 零需求停机、免费应力、无来源空转

**原子应力拒绝**:
内部应力需求超过选定外部来源的可用容量时，本次分配整体失败并保持传动拓扑，由 Create 的应力过载语义停止有效运行。
_Avoid_: 部分供能、合并多个外部网络、破坏传动方块

**外部动力选择**:
工厂在当前有效候选中选定的单个外部动力网络，用于决定内部应力端口镜像的 RPM；选择状态是非持久化的运行时状态，不等同于占用外部应力。
_Avoid_: 外部应力预留、持久化动力来源、多个同时选定来源

**外部应力预留**:
工厂为正内部应力需求向当前选定外部动力网络登记的原子 SU 负载；零容量空转只有外部动力选择，没有外部应力预留。
_Avoid_: 来源选择、零 SU 虚假预留、部分预留

**跨空间转速映射**:
内部应力端口跟随当前选定外部来源的转速大小，同时保留各自内部传动网络已经建立的旋转方向；两个空间之间不传递转速正负号。
_Avoid_: 复制有符号外部转速、首次接入后冻结 RPM

**单源最大余量选择**:
每次应力结算只选择剩余容量最大的一个有效外部动力网络；容量相等时保留当前选定来源，没有当前来源时使用固定接入面顺序，但不合并多个网络的容量。
_Avoid_: 多源合并、随机平局、把相等时保留误作来源亲和

**应力端口转速镜像**:
同一房间的全部应力端口镜像当前选定外部来源的转速大小，而可消耗的 SU 容量仍按各个不同内部网络的真实需求分别分配。
_Avoid_: 仅有负载的端口旋转、每端口复制完整应力容量

**拓扑驱动索引**:
房间参与者索引只在结构变化、模式切换、扩缩容或显式重建时更新，不进行周期性完整拓扑复核。
_Avoid_: 周期性全量复核、分片体积扫描

**结构快照功率/库存**:
功率参与者和库存处理器的统计值只在索引重建时计算；运行中机器速度、应力和库存内容变化不会自动刷新。
_Avoid_: 每秒轮询、每 tick 轮询

**库存处理器索引**:
房间内暴露 IItemHandler 的方块位置集合；库存统计只遍历该集合，且使用完整 ItemVariant 身份计数。
_Avoid_: 原版容器白名单、全体积库存扫描

**显式索引重建**:
玩家通过工厂 GUI 或扳手潜行交互在常加载模式请求的一次完整房间扫描；它重建参与者索引并刷新结构快照功率和库存。
_Avoid_: 后台周期复核、无入口的陈旧快照

**排空处理器轮询**:
仅在 BLACKBOX_DRAINING 中每 20 tick 读取已索引 IItemHandler 的槽位，判断库存是否稳定；它不触发房间体积扫描或功率重新计算。
_Avoid_: 排空全体积扫描、手动确认排空、固定等待时间

**双入口索引重建**:
常加载工厂的显式索引重建可由 GUI 按钮或潜行扳手右键触发，两种入口调用同一服务端校验与重建逻辑。
_Avoid_: 仅 GUI、仅扳手、重复重建实现

**父链功率传播**:
子工厂重建功率快照后，沿已记录父引用逐层重建父工厂的功率快照直到根工厂。
_Avoid_: 父级手动逐个刷新、根级全体积重扫

**动态 handler 解析**:
排空轮询保存处理器位置而不保存 IItemHandler 实例；每次轮询重新解析 capability，并移除已消失的处理器位置。
_Avoid_: 永久 handler 引用、轮询全房间

**运行时房间索引**:
由当前房间结构派生的功率参与者和库存处理器位置缓存；它不持久化，工厂加载、模式切换、扩缩容或显式重建时重新生成。
_Avoid_: 存档索引、旧 capability 快照

**房间变更任务**:
将房间构建、扩缩容、清理或销毁拆分为多个 tick 执行的运行任务；任务期间房间锁定且相关 Pocket 区块保持加载。
_Avoid_: 单 tick 全量写方块、可交互的半完成房间

**持久化房间任务**:
房间变更或验证任务的类型、目标、游标和锁定状态写入持久化状态，使服务器重启后继续收敛到同一房间结果。
_Avoid_: 重启后重做、半完成任务丢失

**最终房间同步**:
房间变更任务以低更新标志分批写入服务端，全部完成后按受影响区块统一同步客户端和相邻方块。
_Avoid_: 每方块客户端同步、永不客户端刷新

**坍缩验证任务**:
在清理任何房间板块前分 tick 检查目标范围的方块、流体和实体；发现第一个冲突即停止并报告原因。
_Avoid_: 单 tick 全板扫描、未验证坍缩

**房间变更预算**:
管理员配置的每 tick 最大方块处理量，用于限制房间验证和变更任务的主线程工作。
_Avoid_: 硬编码预算、TPS 自适应预算

**房间任务管理器**:
世界级 SavedData 模块，持久化并调度所有房间验证和变更任务；销毁任务持有完整房间快照，因此工厂方块实体消失后仍可完成清理。
_Avoid_: 方块实体专属任务、销毁即时清理

**活动房间锁**:
房间任务管理器对正在验证或变更的房间持有的运行锁；结构事件在锁定期间不触发索引重建，任务结束后统一标记一次索引更新。
_Avoid_: 每批重建索引、永不刷新索引

**任务化拆除**:
普通玩家拆除先创建持久销毁任务，但不取消原版破坏，让工厂方块立即消失；任务持有完整房间快照并在后台继续清理。不可取消的方块替换使用同一销毁任务路径。
_Avoid_: 取消玩家破坏导致的客户端方块回弹、销毁任务丢失

**不可取消变更阶段**:
坍缩验证成功后进入的实际构建、清理、扩缩容或销毁任务必须持续到完成；服务器重启后继续，不能尝试世界回滚。
_Avoid_: 中途回滚、半完成房间取消

**玩家可见文本**:
所有由模组生成并展示给玩家的消息、界面标签、tooltip、任务状态和失败原因；必须使用翻译键而不是硬编码语言或 § 格式码。
_Avoid_: 硬编码中文、硬编码英文、格式码字符串

**显式 MOD 总线注册**:
在主模组构造函数中将配置加载等生命周期监听器显式注册到 modEventBus，不依赖 MOD 总线 `@EventBusSubscriber` 自动发现。
_Avoid_: 已弃用的 MOD 总线注解注册、隐式配置监听

**客户端生命周期注册**:
主模组根据注入的运行端 Dist 仅在客户端显式注册屏幕、渲染器和视觉化监听器；专服不挂接客户端类。
_Avoid_: MOD 总线客户端注解、服端加载客户端监听器
